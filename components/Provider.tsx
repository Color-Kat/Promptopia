import React, {memo, FC} from 'react';

interface ProviderProps {

}

export const Provider: FC<ProviderProps> = memo(({}) => {


    return (
        <div className="">
            Provider
        </div>
    );
});