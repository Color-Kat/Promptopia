import React, {memo, FC} from 'react';

interface ProfileProps {

}

export const Profile: FC<ProfileProps> = memo(({}) => {


    return (
        <div className="">
            Profile
        </div>
    );
});