import React, {memo, FC} from 'react';

interface FormProps {

}

export const Form: FC<FormProps> = memo(({}) => {


    return (
        <div className="">
            Form
        </div>
    );
});